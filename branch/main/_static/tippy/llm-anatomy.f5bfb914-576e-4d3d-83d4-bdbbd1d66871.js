selector_to_html = {"a[href=\"https://en.wikipedia.org/wiki/Transformer_(deep_learning)\"]": "<img src=\"https://thumb.wikimedia.org/wikipedia/commons/thumb/3/34/Transformer%2C_full_architecture.png/330px-Transformer%2C_full_architecture.png?utm_source=en.wikipedia.org&utm_campaign=api&utm_content=thumbnail\" alt=\"Wikipedia thumbnail\" style=\"float:left; margin-right:10px;\"><p>In deep learning, the <b>transformer</b> is a family of artificial neural network architectures based on the multi-head attention mechanism, in which input data such as text, images, or audio, is converted to a sequence of numerical representations called tokens, and each token is converted into a vector via lookup from a word embedding table. At each layer, each token is then contextualized within the scope of the context window with other (unmasked) tokens via a parallel multi-head attention mechanism, allowing the signal for key tokens to be amplified and less important tokens to be diminished. Because self-attention alone is permutation-invariant, transformers inject positional information, typically through positional encodings or learned positional embeddings, so token order can affect the output.</p>", "a[href^=\"https://en.wikipedia.org/wiki/Transformer_(deep_learning)#\"]": "<img src=\"https://thumb.wikimedia.org/wikipedia/commons/thumb/3/34/Transformer%2C_full_architecture.png/330px-Transformer%2C_full_architecture.png?utm_source=en.wikipedia.org&utm_campaign=api&utm_content=thumbnail\" alt=\"Wikipedia thumbnail\" style=\"float:left; margin-right:10px;\"><p>In deep learning, the <b>transformer</b> is a family of artificial neural network architectures based on the multi-head attention mechanism, in which input data such as text, images, or audio, is converted to a sequence of numerical representations called tokens, and each token is converted into a vector via lookup from a word embedding table. At each layer, each token is then contextualized within the scope of the context window with other (unmasked) tokens via a parallel multi-head attention mechanism, allowing the signal for key tokens to be amplified and less important tokens to be diminished. Because self-attention alone is permutation-invariant, transformers inject positional information, typically through positional encodings or learned positional embeddings, so token order can affect the output.</p>", "a[href=\"https://en.wikipedia.org/wiki/Byte-pair_encoding\"]": "<p>In computing, <b>byte-pair encoding</b> (<b>BPE</b>), or <b>digram coding</b>, is an algorithm, first described in 1994 by Philip Gage, for encoding strings of text into smaller strings by creating and using a translation table. A slightly modified version of the algorithm is used in large language model tokenizers.</p>", "a[href^=\"https://en.wikipedia.org/wiki/Byte-pair_encoding#\"]": "<p>In computing, <b>byte-pair encoding</b> (<b>BPE</b>), or <b>digram coding</b>, is an algorithm, first described in 1994 by Philip Gage, for encoding strings of text into smaller strings by creating and using a translation table. A slightly modified version of the algorithm is used in large language model tokenizers.</p>", "a[href=\"https://en.wikipedia.org/wiki/Word2Vec\"]": "<p>In natural language processing, <b>Word2Vec</b> is a technique for obtaining vector representations of words. These vectors capture information about the meaning of the word based on the surrounding words, following the principles of distributional semantics. Once trained, the model can be used to find words with similar meanings or usage, while its embeddings can serve as inputs to systems for search and classification.</p>", "a[href^=\"https://en.wikipedia.org/wiki/Word2Vec#\"]": "<p>In natural language processing, <b>Word2Vec</b> is a technique for obtaining vector representations of words. These vectors capture information about the meaning of the word based on the surrounding words, following the principles of distributional semantics. Once trained, the model can be used to find words with similar meanings or usage, while its embeddings can serve as inputs to systems for search and classification.</p>", "a[href=\"#id2\"]": "<figure class=\"align-default\" id=\"id2\">\n<a class=\"img-responsive reference internal image-reference\" href=\"../_images/transformer-block.png\"><img alt=\"../_images/transformer-block.png\" class=\"img-responsive\" src=\"../_images/transformer-block.png\" style=\"width: 70%;\"/>\n</a>\n<figcaption>\n<p><span class=\"caption-text\">The original transformer architecture. Modern large language models often use\ndecoder-only variants, but retain the repeated pattern of attention,\nfeed-forward layers, residual connections, and normalization. <a class=\"reference external\" href=\"https://en.wikipedia.org/wiki/Transformer_(deep_learning)#/media/File:Transformer,_full_architecture.png\">Source</a></span><a class=\"headerlink\" href=\"#id2\" title=\"Link to this image\">\u00b6</a></p>\n</figcaption>\n</figure>", "a[href=\"#training-pipeline\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Training pipeline<a class=\"headerlink\" href=\"#training-pipeline\" title=\"Link to this heading\">\u00b6</a></h2><p>When a LLM is first trained, three steps are usually involved:</p>", "a[href=\"#why-tokenisation-matters\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Why tokenisation matters<a class=\"headerlink\" href=\"#why-tokenisation-matters\" title=\"Link to this heading\">\u00b6</a></h3><p>Tokenisation affects the length of the sequence seen by the model. This in\nturn affects memory use, computation, and the amount of text that fits within\nthe context window.</p><p>It can also affect performance across languages and specialist domains. A\ntokeniser may represent frequent English words efficiently while dividing a\nscientific term or a word from another language into many small fragments.\nThe model can still process such text, but the representation uses more\npositions and may have been encountered less often during training.</p>", "a[href=\"#encoder-only-models\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Encoder-only models<a class=\"headerlink\" href=\"#encoder-only-models\" title=\"Link to this heading\">\u00b6</a></h3><p>An encoder-only model builds contextual representations using information\nfrom both sides of a token. This is useful when the model must understand an\nentire input rather than generate a continuation.</p><p>Encoder-only models are commonly associated with classification,\nnamed-entity recognition, semantic similarity, and extractive question\nanswering.</p>", "a[href=\"#from-representations-to-token-probabilities\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">From representations to token probabilities<a class=\"headerlink\" href=\"#from-representations-to-token-probabilities\" title=\"Link to this heading\">\u00b6</a></h2><p>After the final transformer block, the model converts the representation at\nthe relevant sequence position into one score for every token in the\nvocabulary. These scores are called <strong>logits</strong>.</p><p>Softmax turns the logits into a probability distribution:</p>", "a[href=\"#a-concrete-interpretation\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">A concrete interpretation<a class=\"headerlink\" href=\"#a-concrete-interpretation\" title=\"Link to this heading\">\u00b6</a></h3><p>Suppose the model processes:</p>", "a[href=\"#exercise-attention-and-masking\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Exercise: Attention and masking<a class=\"headerlink\" href=\"#exercise-attention-and-masking\" title=\"Link to this heading\">\u00b6</a></h2>", "a[href=\"#sidenote-context-length-and-computational-cost\"]": "<h4 class=\"tippy-header\" style=\"margin-top: 0;\">Sidenote: Context length and computational cost<a class=\"headerlink\" href=\"#sidenote-context-length-and-computational-cost\" title=\"Link to this heading\">\u00b6</a></h4><p>For standard dense attention, a sequence containing (n) tokens produces an\nattention-score matrix containing (n^2) entries for every head.</p><p>Doubling the sequence length therefore produces approximately four times as\nmany query-key comparisons. The total cost of a transformer also includes\nprojection and feed-forward operations, so end-to-end scaling is more\ncomplicated than one formula suggests. Nevertheless, the quadratic attention\nmatrix is an important reason why long contexts can be expensive. There are a\nfew tricks to mitigate this, such as <a class=\"reference external\" href=\"https://huggingface.co/blog/not-lain/kv-caching\">KV\ncaching</a>.</p>", "a[href=\"#from-token-ids-to-embeddings\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">From token IDs to embeddings<a class=\"headerlink\" href=\"#from-token-ids-to-embeddings\" title=\"Link to this heading\">\u00b6</a></h2><p>A token ID is an integer label. The numerical distance between two token IDs\nhas no semantic meaning. Token ID 100 is not inherently more similar to token\nID 101 than it is to token ID 9000.</p><p>Before the transformer can process the tokens, the model maps every token ID\nto a vector. This operation is performed by an embedding layer.</p>", "a[href=\"#exercise-trace-information-through-the-model\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Exercise: Trace information through the model<a class=\"headerlink\" href=\"#exercise-trace-information-through-the-model\" title=\"Link to this heading\">\u00b6</a></h2>", "a[href=\"#from-text-to-tokens\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">From text to tokens<a class=\"headerlink\" href=\"#from-text-to-tokens\" title=\"Link to this heading\">\u00b6</a></h2><p>The first step is tokenisation. A tokeniser divides the input into units\ncalled <strong>tokens</strong> and maps every token to an integer ID.</p><p>Tokens do not necessarily correspond to words. Depending on the tokeniser, a\ntoken may represent a complete word, part of a word, punctuation, whitespace,\nor another frequently occurring character sequence.</p>", "a[href=\"#from-values-to-attention\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">From values to attention<a class=\"headerlink\" href=\"#from-values-to-attention\" title=\"Link to this heading\">\u00b6</a></h3><p><img alt=\"\" src=\"../_images/value-fat-ginger.png\"/></p><p>Value matrix multiplied by the embedding of the previous word results in the value vector</p>", "a[href=\"#query-key-dot-product\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Query-Key dot product<a class=\"headerlink\" href=\"#query-key-dot-product\" title=\"Link to this heading\">\u00b6</a></h3><p><img alt=\"\" src=\"../_images/query-key-fat-ginger.png\"/></p><p>Compute the dot product with each query-key pair, to determine how well the key matches the query.\nWhere the queries and keys align, the dot product is larger.\nLower left dot products are masked, as we want the model to predict every next word during training. To prevent data leakage, future words should not influence previous ones.</p>", "a[href=\"#comparing-queries-and-keys\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Comparing queries and keys<a class=\"headerlink\" href=\"#comparing-queries-and-keys\" title=\"Link to this heading\">\u00b6</a></h3><p>The model computes a dot product between each query and each key. A larger\ndot product means that the query and key are more strongly aligned in the\nlearned representation space.</p><p>The scaled dot-product attention operation is:</p>", "a[href=\"#self-attention\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Self-attention<a class=\"headerlink\" href=\"#self-attention\" title=\"Link to this heading\">\u00b6</a></h3><p>Self-attention allows the representation at one token position to incorporate\ninformation from other token positions in the same sequence.</p><p>For every token, an attention head computes three vectors:</p>", "a[href=\"#id1\"]": "<figure class=\"align-default\" id=\"id1\">\n<a class=\"reference internal image-reference\" href=\"../_images/encoder-decoder-text2img.png\"><img alt=\"Encoder-decoder model\" src=\"../_images/encoder-decoder-text2img.png\" style=\"width: 100%;\"/>\n</a>\n<figcaption>\n<p><span class=\"caption-text\">Text to image generative model as an example of an encoder-decoder model. Think\n<em>DALL-E, Stable Diffusion, FLUX.1 [dev] etc.</em>.</span><a class=\"headerlink\" href=\"#id1\" title=\"Link to this image\">\u00b6</a></p>\n</figcaption>\n</figure>", "a[href=\"#initial-and-contextual-representations\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Initial and contextual representations<a class=\"headerlink\" href=\"#initial-and-contextual-representations\" title=\"Link to this heading\">\u00b6</a></h3><p>The embedding-layer output is only the initial representation of a token. It\ndoes not yet express what the token means in a particular sentence.</p><p>Consider the word <code class=\"docutils literal notranslate\"><span class=\"pre\">mole</span></code>:</p>", "a[href=\"#feed-forward-networks\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Feed-forward networks<a class=\"headerlink\" href=\"#feed-forward-networks\" title=\"Link to this heading\">\u00b6</a></h2><p>After attention has exchanged information between token positions, a\nfeed-forward network transforms the representation at each position.</p><p>A simplified feed-forward operation is:</p>", "a[href=\"#causal-masking\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Causal masking<a class=\"headerlink\" href=\"#causal-masking\" title=\"Link to this heading\">\u00b6</a></h3><p>During next-token training, a decoder-only model must not use future tokens to\npredict an earlier token.</p><p>Consider:</p>", "a[href=\"#inside-a-transformer-block\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Inside a transformer block<a class=\"headerlink\" href=\"#inside-a-transformer-block\" title=\"Link to this heading\">\u00b6</a></h2><p>A transformer contains a stack of repeated blocks. Although details differ\nbetween model families, each block contains two major computational\ncomponents:</p>", "a[href=\"#position-in-the-sequence\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Position in the sequence<a class=\"headerlink\" href=\"#position-in-the-sequence\" title=\"Link to this heading\">\u00b6</a></h3><p>Attention alone does not inherently know whether one token comes before or\nafter another. The model must therefore include information about position.</p><p>The original transformer added positional encodings to token embeddings.\nModern architectures may use other methods, such as learned position\nembeddings or rotary position embeddings.</p>", "a[href=\"#type-of-transformers\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Type of transformers<a class=\"headerlink\" href=\"#type-of-transformers\" title=\"Link to this heading\">\u00b6</a></h2><p>Transformer architectures are commonly grouped into three broad families.</p>", "a[href=\"#encoder-decoder-models\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Encoder-decoder models<a class=\"headerlink\" href=\"#encoder-decoder-models\" title=\"Link to this heading\">\u00b6</a></h3><p>An encoder-decoder model first builds representations of the input with an\nencoder. A decoder then generates an output while attending to both the\nalready generated output and the encoded input.</p><p>This architecture is well suited for sequence-to-sequence tasks such as\ntranslation and summarization. They are also used for\nmultimodal tasks such as image segmentation (for eg., Detectron) and generation (for eg., Stable Diffusion).</p>", "a[href=\"#residual-connections-and-normalization\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Residual connections and normalization<a class=\"headerlink\" href=\"#residual-connections-and-normalization\" title=\"Link to this heading\">\u00b6</a></h2><p>A transformer block does not replace its input representation outright.\nResidual connections allow a sublayer to contribute an update:</p>", "a[href=\"#decoder-only-models\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Decoder-only models<a class=\"headerlink\" href=\"#decoder-only-models\" title=\"Link to this heading\">\u00b6</a></h3><p>A decoder-only model predicts the next token based on the tokens that precede\nit. Most contemporary general-purpose generative language models use this\narchitecture.</p><p>At each generation step, the model produces a probability distribution over\nthe vocabulary. A decoding procedure selects a token, appends it to the\nsequence, and invokes the model again.</p>", "a[href=\"#anatomy-of-a-large-language-model\"]": "<h1 class=\"tippy-header\" style=\"margin-top: 0;\">Anatomy of a Large Language Model<a class=\"headerlink\" href=\"#anatomy-of-a-large-language-model\" title=\"Link to this heading\">\u00b6</a></h1><p>A language model does not read text as words and sentences: text must first be\nconverted into numerical representations, then transformer layers update\nthose representations using the surrounding context.</p><p>A simplified forward pass looks like this:</p>", "a[href=\"#multi-head-attention\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Multi-head attention<a class=\"headerlink\" href=\"#multi-head-attention\" title=\"Link to this heading\">\u00b6</a></h2><p>A transformer does not compute a single attention pattern. It computes\nseveral attention heads in parallel.</p><p>Each head has its own query, key, and value projections. This allows different\nheads to construct different patterns of information exchange. The outputs of\nthe heads are concatenated and passed through another learned projection.</p>", "a[href=\"#summary\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Summary<a class=\"headerlink\" href=\"#summary\" title=\"Link to this heading\">\u00b6</a></h2><p>A language model begins by dividing text into tokens. Token IDs are mapped to\ninitial vectors, and positional information records where those tokens occur\nin the sequence.</p><p>Transformer blocks repeatedly update the representations. Attention exchanges\ninformation between token positions. Queries and keys determine which\npositions are relevant, while values contain the information that is\ncombined. Causal masking prevents a decoder-only model from using future\ntokens.</p>", "a[href=\"#why-not-use-one-token-per-word\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Why not use one token per word?<a class=\"headerlink\" href=\"#why-not-use-one-token-per-word\" title=\"Link to this heading\">\u00b6</a></h3><p>A word-level vocabulary is simple to imagine:</p>"}
skip_classes = ["headerlink", "sd-stretched-link"]

window.onload = function () {
    for (const [select, tip_html] of Object.entries(selector_to_html)) {
        const links = document.querySelectorAll(` ${select}`);
        for (const link of links) {
            if (skip_classes.some(c => link.classList.contains(c))) {
                continue;
            }

            tippy(link, {
                content: tip_html,
                allowHTML: true,
                arrow: true,
                placement: 'auto-start', maxWidth: 500, interactive: false,

            });
        };
    };
    console.log("tippy tips loaded!");
};
